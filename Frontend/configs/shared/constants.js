export const SERVER = {
    HOST: true,
    PORT: 5173,
    STRICT_PORT: true,
};

export const DEV_BUILD = {
    EMPTY_OUT_DIR: true,
    SOURCE_MAP: true,
    MINIFY: false,
};

export const LIVE_BUILD = {
    EMPTY_OUT_DIR: true,
};